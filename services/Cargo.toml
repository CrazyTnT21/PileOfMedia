[package]
name = "services"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1.83"
domain = { path = "../domain" }

[lib]
name = "services"
path = "lib.rs"

[lints]
workspace = true
